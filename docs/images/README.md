# README images

- `architecture.svg` — system architecture diagram (committed, renders in the
  READMEs).
- Screenshots referenced by the READMEs (drop real PNGs here with these names):
  - `dashboard.png` — 总览 / 首页(卡片 + 最近运行 + SLA 趋势)
  - `run-live.png` — 实时压测页(QPS / 延迟 / 错误率 + 实时日志)
  - `compare.png` — 结果对比(并排 + 涨跌色标)
  - `builder.png` — 测试用例 / 场景构建器

建议宽度 ~1400px、深色主题截图,观感最佳。
